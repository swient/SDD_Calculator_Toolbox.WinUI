<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CalculatorToolbox.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalculatorToolbox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="計算工具箱">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="#F3F3F3">
        <NavigationView x:Name="MainNavView"
                        PaneDisplayMode="LeftMinimal"
                        IsBackButtonVisible="Visible"
                        IsSettingsVisible="True"
                        VerticalAlignment="Stretch">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="標準" Tag="Standard">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Calculator"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="科學" Tag="Scientific">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Library"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="程式設計人員" Tag="Programmer">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Keyboard"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.Content>
                <Frame x:Name="ContentFrame">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition/>
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Window>
